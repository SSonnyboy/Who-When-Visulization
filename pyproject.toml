[project]
name = "who-when-visualization-worker"
version = "0.1.0"
description = "Cloudflare Python Worker backend for the Who&When dashboard."
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
